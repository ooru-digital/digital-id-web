apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-id-web.fullname" . }}
  labels:
    {{- include "digital-id-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digital-id-web.selectorLabels" . | nindent 4 }}
