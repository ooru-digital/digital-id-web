apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "digital-id-web.fullname" . }}-test-connection"
  labels:
    {{- include "digital-id-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "digital-id-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
